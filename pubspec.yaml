name: a2_tutorial
description: A new Flutter project.

# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  video_player: ^2.5.0
  hive: ^2.2.3
  hive_flutter: ^1.1.0
  # audio_visualizer: ^0.1.1
  mic_stream: ^0.6.2
  raw_sound: ^0.3.0+1
  noise_meter: ^4.0.1
  url_launcher: ^6.1.8
  path_provider: ^2.0.13
  provider: ^6.0.5
  # sura_manager: ^1.5.0
  # image_downloader: ^0.30.0
  uuid: ^4.0.0
  fluttertoast: ^8.2.1
  dio: ^5.0.3
  permission_handler: ^10.2.0
  open_filex: ^4.3.2
  package_info_plus: ^4.1.0
  package_config: ^2.1.0
  yaml: ^3.1.1
  video_editor: ^3.0.0
  file_picker: ^5.2.10
  image_picker: ^1.0.4
  infinite_scroll_pagination: ^4.0.0
  # sura_flutter: ^1.4.2
  soriya_flutter:
    git:
      url: https://github.com/Sovann72/soriya-flutter.git
      branch: stable
      # branch: null-safety-migration
  sura_manager:
    git:
      url: https://github.com/Sovann72/sura-manager.git
      branch: master
  sura_flutter:
    git:
      url: https://github.com/Sovann72/sura-flutter.git
      branch: master

  # helper: ^0.0.6

# dependency_overrides:
#   ffmpeg_kit_flutter_min_gpl: ^5.1.0-LTS

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^2.0.0
  hive_generator: ^2.0.0
  build_runner: ^2.3.3

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:
  uses-material-design: true

  assets:
    - assets/mp3/
